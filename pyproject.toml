[project]
name = "hebz"
version = "0.1.0"
description = "Accurate Helium-Benzene Potential: from CCSD(T) to Gaussian Process Regression"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
]
